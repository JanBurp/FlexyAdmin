/**
 * Dit bestand voor in de backend geladen.
 * Je kunt hiermee dus extra features toevoegen.
 * Let op: de backed kan veranderen, dus check na elke update of je extra features nog werken
 */
$(document).ready(function() {
});
